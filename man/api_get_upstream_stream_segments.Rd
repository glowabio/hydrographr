% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api_get_upstream_stream_segments.R
\name{api_get_upstream_stream_segments}
\alias{api_get_upstream_stream_segments}
\title{Get Upstream Stream Segments}
\usage{
api_get_upstream_stream_segments(
  subc_id = NULL,
  lon = NULL,
  lat = NULL,
  geometry_only = FALSE,
  add_upstream_ids = FALSE,
  comment = NULL
)
}
\arguments{
\item{subc_id}{Integer. A sub-catchment ID from which to trace upstream.
One of `subc_id` or (`lon` + `lat`) must be provided.}

\item{lon}{Numeric. Longitude of a point (used together with `lat`).}

\item{lat}{Numeric. Latitude of a point (used together with `lon`).}

\item{geometry_only}{Logical. If `TRUE`, returns only the geometry of the stream
segments without associated attributes. Defaults to `FALSE`.}

\item{add_upstream_ids}{Logical. If `TRUE`, upstream sub-catchment IDs are
added to the output. Defaults to `FALSE`.}

\item{comment}{Character. Optional comment for API logging.}
}
\value{
An `sf` object representing the upstream stream segments.
}
\description{
This function returns all stream segments upstream of a specified location.
The location can be identified by one of: `subc_id`, or a pair of
coordinates (`lon` + `lat`). All inputs are sent as JSON.
}
\details{
Sends a request to the GeoFRESH API to retrieve all stream segments
upstream of a given point or sub-catchment and returns them as an `sf` object.
}
\examples{
\dontrun{
# Using lon/lat (Sarantaporos outlet)
upstream_sf <- api_get_upstream_stream_segments(
  lon = 20.538704,
  lat = 40.113735,
  geometry_only = FALSE,
  add_upstream_ids = TRUE
)

# Using subc_id
upstream_sf <- api_get_upstream_stream_segments(
  subc_id = 506586041,
  geometry_only = FALSE
)

library(leaflet)
leaflet(upstream_sf) |>
  addProviderTiles("CartoDB.Positron") |>
  addPolylines(color = "blue")
}

}
\seealso{
Other ocgapi: 
\code{\link{api_filter_by_attribute}()},
\code{\link{api_get_basin_polygon}()},
\code{\link{api_get_basin_streamsegments}()},
\code{\link{api_get_basin_subcids}()},
\code{\link{api_get_env90m_data}()},
\code{\link{api_get_job_results}()},
\code{\link{api_get_local_ids}()},
\code{\link{api_get_snapped_points}()},
\code{\link{api_get_snapped_points_cascade}()},
\code{\link{api_get_snapped_points_df}()},
\code{\link{api_get_snapped_points_strahler_df}()},
\code{\link{api_get_upstream_catchment}()},
\code{\link{api_get_upstream_subcids}()}
}
\concept{ocgapi}
