% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api_get_snapped_points_strahler_df.R
\name{api_get_snapped_points_strahler_df}
\alias{api_get_snapped_points_strahler_df}
\title{Snap Points to River Network Using GeoJSON Input}
\usage{
api_get_snapped_points_strahler_df(
  data,
  colname_lon = "longitude",
  colname_lat = "latitude",
  colname_site_id = "site_id",
  min_strahler = NULL,
  add_distance = TRUE,
  force_async = NULL,
  async_threshold = 500,
  poll_interval = 10,
  max_wait = 3600
)
}
\arguments{
\item{data}{A data.frame containing point data with coordinates and site IDs.}

\item{colname_lon}{Character. Name of the longitude column in the data.frame.
Default: "longitude".}

\item{colname_lat}{Character. Name of the latitude column in the data.frame.
Default: "latitude".}

\item{colname_site_id}{Character. Name of the site ID column in the data.frame.
Default: "site_id".}

\item{min_strahler}{Integer. Minimum Strahler order to snap to (e.g., 5).}

\item{add_distance}{Logical. If TRUE, includes a 'distance_metres' column with
the snapping distance in meters. Default: TRUE.}

\item{force_async}{Logical. If TRUE, forces async mode. If FALSE, forces sync.
If NULL (default), uses async for datasets >= async_threshold.}

\item{async_threshold}{Numeric. Number of points threshold for auto-async.
Default: 500.}

\item{poll_interval}{Numeric. Seconds between status checks when polling
async jobs. Default: 10.}

\item{max_wait}{Numeric. Maximum wait time in seconds for async jobs.
Default: 3600 (1 hour).}
}
\value{
A data.frame with the snapped output including:
  - Original columns from input data
  - longitude_snapped, latitude_snapped (snapped coordinates)
  - subc_id (subcatchment ID)
  - strahler (Strahler order of snapped segment)
  - distance_metres (if add_distance = TRUE)
}
\description{
This function accepts a data.frame with coordinates and converts it to GeoJSON
format for the API. Unlike the CSV version, this doesn't require uploading
files to a server first. For datasets with >= 500 points (or custom threshold),
automatically uses async mode with built-in polling.
}
\details{
Sends point data as GeoJSON to the GeoFRESH API process
`get-snapped-points-strahler-plural`, which snaps each point to the nearest
stream segment above a given minimum Strahler order.
}
\examples{
\dontrun{
# Small dataset (sync mode)
sites <- data.frame(
  site_id = c("site_1", "site_2", "site_3"),
  longitude = c(9.931555, 9.921555, 9.941555),
  latitude = c(54.695070, 54.295070, 54.495070),
  species = c("Trout", "Salmon", "Pike")
)

result <- api_get_snapped_points_strahler_df(
  data = sites,
  min_strahler = 4,
  add_distance = TRUE
)

# Large dataset (auto-async)
gbif_data <- fread("my_fish_occurrences.csv")

result <- api_get_snapped_points_strahler_df(
  data = gbif_data,
  colname_lon = "decimalLongitude",
  colname_lat = "decimalLatitude",
  colname_site_id = "gbifID",
  min_strahler = 4
)

# Force async mode
result <- api_get_snapped_points_strahler_df(
  data = sites,
  min_strahler = 4,
  force_async = TRUE
)

# Custom threshold (async if > 100 points)
result <- api_get_snapped_points_strahler_df(
  data = sites,
  min_strahler = 4,
  async_threshold = 100
)
}
}
\seealso{
Other ocgapi: 
\code{\link{api_filter_by_attribute}()},
\code{\link{api_get_basin_polygon}()},
\code{\link{api_get_basin_streamsegments}()},
\code{\link{api_get_basin_subcids}()},
\code{\link{api_get_env90m_data}()},
\code{\link{api_get_job_results}()},
\code{\link{api_get_local_ids}()},
\code{\link{api_get_snapped_points}()},
\code{\link{api_get_snapped_points_cascade_async}()},
\code{\link{api_get_snapped_points_cascade_df}()},
\code{\link{api_get_snapped_points_df}()},
\code{\link{api_get_snapped_points_strahler_async}()},
\code{\link{api_get_snapped_points_strahler_plural}()},
\code{\link{api_get_upstream_subcids}()}
}
\author{
Afroditi Grigoropoulou, Merret Buurman
}
\concept{ocgapi}
