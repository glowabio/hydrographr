% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api_get_snapped_points_strahler_async.R
\name{api_get_snapped_points_strahler_async}
\alias{api_get_snapped_points_strahler_async}
\title{Snap Many Points to River Network Using Strahler Threshold (CSV Input)}
\usage{
api_get_snapped_points_strahler_async(
  csv_url,
  colname_lon = "longitude",
  colname_lat = "latitude",
  colname_site_id = "site_id",
  min_strahler = NULL,
  add_distance = FALSE,
  force_async = NULL,
  async_threshold = 1000
)
}
\arguments{
\item{csv_url}{URL to a CSV file containing the point data.}

\item{colname_lon}{Name of the longitude column inside the CSV. Default: "longitude".}

\item{colname_lat}{Name of the latitude column inside the CSV. Default: "latitude".}

\item{colname_site_id}{Name of the site ID column inside the CSV. Default: "site_id".}

\item{min_strahler}{Minimum Strahler order to snap to (e.g., 5).}

\item{add_distance}{Logical. If TRUE, includes a 'distance_metres' column with the snapping
distance in meters. Default: FALSE.}

\item{force_async}{Logical. NULL = auto-detect based on dataset size (default),
TRUE = force async mode, FALSE = force sync mode.}

\item{async_threshold}{Numeric. Number of points threshold for auto-async (default: 1000).}
}
\value{
A list with:
  \describe{
    \item{data}{A data.frame with the snapped output (if sync mode)}
    \item{href}{The download link returned by the API (if sync mode)}
    \item{jobID}{Job ID for async jobs (if async mode)}
    \item{status}{Job status (if async mode)}
    \item{async}{Logical indicating whether async mode was used}
  }
}
\description{
Sends a CSV file URL with multiple points to the GeoFRESH API process
`get-snapped-points-strahler-plural`, which snaps each point to the nearest
stream segment above a given minimum Strahler order.
}
\details{
For large datasets (>= async_threshold points), automatically uses async mode.
In async mode, the function returns a job object that can be polled for results
using api_poll_job().
}
\examples{
\dontrun{
# Small dataset (sync mode)
result <- api_get_snapped_points_strahler(
  csv_url = "https://aqua.igb-berlin.de/referencedata/aqua90m/spdata_barbus.csv",
  min_strahler = 5,
  add_distance = TRUE
)
head(result$data)

# Large dataset (async mode)
job <- api_get_snapped_points_strahler(
  csv_url = "https://example.com/large_dataset.csv",
  min_strahler = 4,
  add_distance = TRUE
)
# Poll for results
result <- api_poll_job(job$jobID)
df <- api_get_job_results(result$href)
}
}
\seealso{
Other ocgapi: 
\code{\link{api_filter_by_attribute}()},
\code{\link{api_get_basin_polygon}()},
\code{\link{api_get_basin_streamsegments}()},
\code{\link{api_get_basin_subcids}()},
\code{\link{api_get_env90m_data}()},
\code{\link{api_get_job_results}()},
\code{\link{api_get_local_ids}()},
\code{\link{api_get_snapped_points}()},
\code{\link{api_get_snapped_points_cascade}()},
\code{\link{api_get_snapped_points_cascade_async}()},
\code{\link{api_get_snapped_points_df}()},
\code{\link{api_get_snapped_points_strahler_df}()},
\code{\link{api_get_snapped_points_strahler_plural}()},
\code{\link{api_get_upstream_subcids}()}
}
\author{
Afroditi Grigoropoulou, Merret Buurman
}
\concept{ocgapi}
