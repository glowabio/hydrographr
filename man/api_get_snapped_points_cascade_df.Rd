% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api_get_snapped_points_cascade_df.R
\name{api_get_snapped_points_cascade_df}
\alias{api_get_snapped_points_cascade_df}
\title{Snap Points to Stream Network (Cascade Workflow) - JSON Input}
\usage{
api_get_snapped_points_cascade_df(
  data,
  colname_lon = "longitude",
  colname_lat = "latitude",
  colname_site_id = "site_id",
  strahler_seq = c(4, 3, 2),
  distance_threshold = 150,
  async_threshold = 500,
  poll_interval = 10,
  max_wait = 7200
)
}
\arguments{
\item{data}{A data.frame containing point data with coordinates and site IDs.}

\item{colname_lon}{Character. Longitude column name in data.frame.
Default: `"longitude"`.}

\item{colname_lat}{Character. Latitude column name in data.frame.
Default: `"latitude"`.}

\item{colname_site_id}{Character. Site ID column name in data.frame.
Default: `"site_id"`.}

\item{strahler_seq}{Integer vector. Descending Strahler orders to try
(e.g., `c(4, 3, 2)`). Higher values tried first.}

\item{distance_threshold}{Numeric. Maximum snapping distance in meters.
Points exceeding this are re-snapped to next lower Strahler level.
Default: `150`.}

\item{async_threshold}{Numeric. Number of points threshold for auto-async.
Default: `500`.}

\item{poll_interval}{Numeric. Seconds between async job status checks.
Default: `10`.}

\item{max_wait}{Numeric. Maximum seconds to wait for job completion.
Default: `7200` (2 hours).}
}
\value{
A data.frame with successfully snapped points containing:
  \describe{
    \item{site_id}{Site identifier from input data.}
    \item{subc_id}{Subcatchment ID of snapped location.}
    \item{strahler}{Strahler order of stream segment.}
    \item{longitude_snapped, latitude_snapped}{Snapped coordinates.}
    \item{longitude_original, latitude_original}{Original coordinates.}
    \item{distance_metres}{Snapping distance in meters.}
    \item{...}{Any other columns from original data.}
  }
  Returns empty data.frame if no points successfully snapped.
}
\description{
Snaps points to stream network across descending Strahler orders using
data.frame input (no CSV upload needed). Points exceeding distance threshold
at higher orders are re-snapped to lower orders until successful or all
levels exhausted.
}
\details{
**Workflow per Strahler level:**
\enumerate{
  \item Convert data.frame to GeoJSON and snap to current Strahler level
  \item Poll until job completes (if async)
  \item Separate points within vs exceeding distance threshold
  \item Store successful snaps, prepare failed snaps for next level
  \item Re-snap failures from original coordinates at next lower Strahler
}

Points always snap from original coordinates to ensure each attempt starts fresh.
}
\examples{
\dontrun{
# Create sample data
sites <- data.frame(
  site_id = c("site_1", "site_2", "site_3"),
  longitude = c(9.931555, 9.921555, 9.941555),
  latitude = c(54.695070, 54.295070, 54.495070),
  species = c("Trout", "Salmon", "Pike")
)

# Run cascade
result <- api_get_snapped_points_cascade_df(
  data = sites,
  strahler_seq = c(5, 4, 3),
  distance_threshold = 200
)

# Large dataset (auto-async)
gbif_data <- fread("my_fish_data.csv")
result <- api_get_snapped_points_cascade_df(
  data = gbif_data,
  colname_lon = "decimalLongitude",
  colname_lat = "decimalLatitude",
  colname_site_id = "gbifID",
  strahler_seq = c(4, 3, 2)
)
}

}
\seealso{
Other ocgapi: 
\code{\link{api_filter_by_attribute}()},
\code{\link{api_get_basin_polygon}()},
\code{\link{api_get_basin_streamsegments}()},
\code{\link{api_get_basin_subcids}()},
\code{\link{api_get_env90m_data}()},
\code{\link{api_get_job_results}()},
\code{\link{api_get_local_ids}()},
\code{\link{api_get_local_ids_async}()},
\code{\link{api_get_snapped_points}()},
\code{\link{api_get_snapped_points_cascade_async}()},
\code{\link{api_get_snapped_points_df}()},
\code{\link{api_get_snapped_points_strahler_async}()},
\code{\link{api_get_snapped_points_strahler_df}()},
\code{\link{api_get_snapped_points_strahler_plural}()},
\code{\link{api_get_upstream_subcids}()}
}
\concept{ocgapi}
